<%= form_with(model: category_product) do |form| %>
  <% if category_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_product.errors.count, "error") %> prohibited this category_product from being saved:</h2>

      <ul>
        <% category_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
