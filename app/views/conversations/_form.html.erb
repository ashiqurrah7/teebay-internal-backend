<%= form_with(model: conversation) do |form| %>
  <% if conversation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
        <% conversation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_user_id_id %>
    <%= form.text_field :first_user_id_id %>
  </div>

  <div class="field">
    <%= form.label :second_user_id_id %>
    <%= form.text_field :second_user_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
